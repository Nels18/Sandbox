import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sandbox.app',
  appName: 'sandbox',
  webDir: 'dist/sandbox/browser',
};

export default config;
